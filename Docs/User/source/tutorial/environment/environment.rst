Setting up environment 
----------------------

Let's start the tutorial by setting up the needed environment to run the examples. For this particular section, it is assumed that a clean CentOS 7 installation is being used (https://www.centos.org/download/). Alternatively, there is also a Dockerfile available (see below).

.. toctree::
   :maxdepth: 2
   :caption: Contents:

   downloading
   building
   docker
   env